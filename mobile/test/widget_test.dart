// Smoke test placeholder — the full app requires Firebase + dotenv
// initialization that cannot run in a unit-test environment.
// Integration tests live in the integration_test/ folder.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app bootstraps correctly in integration tests', () {
    expect(true, isTrue);
  });
}
